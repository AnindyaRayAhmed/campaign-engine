# Use Gemini API key for local runs.
# For Cloud Run, prefer Secret Manager + env var injection.
GOOGLE_API_KEY=your_gemini_api_key_here

# SQLite database file (relative path works for local dev).
CAMPAIGN_DB_PATH=campaigns.db
